---
"@hypequery/datasets": minor
---

Allow dataset queries to select dataset-owned derived measures alongside base measures. Derived values are computed after aggregation and before ordering and pagination, and are included in local dataset catalogs and query schemas.
