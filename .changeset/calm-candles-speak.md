---
"@hypequery/protocol": minor
---

Add a strict v2 dataset-only deployment contract with dataset-owned derived measures, canonical encoding and identity, and a read-only projection for stored v1 releases. Preserve existing v1 contract APIs for self-hosted callers.
