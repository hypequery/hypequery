---
"@hypequery/datasets": patch
"@hypequery/mcp": patch
---

Add dataset catalog metadata for measures, filters, relationships, limits, and attached named metrics. Update MCP dataset introspection and dataset querying metadata to distinguish measures from named metrics while retaining the existing `metrics` query argument as a compatibility alias.
