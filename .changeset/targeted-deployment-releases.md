---
'@hypequery/protocol': minor
'@hypequery/cli': minor
---

Add deterministic target-bound deployment release envelopes and a CLI command
that prepares them only from fully verified deployment bundles.
