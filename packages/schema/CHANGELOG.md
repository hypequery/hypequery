# @hypequery/schema

## 0.1.0

### Minor Changes

- 75349dd: Publish the initial schema and migrations toolkit for ClickHouse.

  The package provides a declarative schema DSL, stable schema snapshots and
  hashing, schema diffing, cost-aware migration plans, SQL artifact rendering,
  materialized-view dependency handling, and compatibility checks between schema
  changes and semantic datasets.
