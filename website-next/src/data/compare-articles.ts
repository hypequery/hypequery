export type CompareArticle = {
  slug: string;
  title: string;
  description: string;
  seoTitle?: string;
  seoDescription?: string;
  date: string;
  tags: string[];
  content: string;
};

export const compareArticles: Record<string, CompareArticle> = {
  "cube-vs-tinybird-vs-hypequery": {
    "slug": "cube-vs-tinybird-vs-hypequery",
    "title": "Cube vs Tinybird vs hypequery: Three Ways to Put an API on ClickHouse",
    "description": "Cube, Tinybird, and hypequery all sit between ClickHouse and your application — but they're a platform, a managed service, and a library. Here's how to actually choose.",
    "seoTitle": "Cube vs Tinybird vs hypequery — ClickHouse Analytics API Comparison",
    "seoDescription": "Comparing Cube, Tinybird, and hypequery for serving ClickHouse analytics: architecture, pricing, type safety, and which one fits your team.",
    "date": "2026-07-03",
    "tags": [],
    "content": "If you search \"Cube vs Tinybird,\" you'll mostly find each vendor explaining why the other one is solving a different problem. Which is actually true — they are. But it's not very helpful when what you have is a ClickHouse full of data, an application that needs analytics, and a decision to make.\n\nSo here's the version with all three options on the table, including the one where you don't adopt a platform at all.\n\n## The one-sentence versions\n\n**Cube** is a semantic layer platform. It sits between your databases (ClickHouse among many) and your consumers (BI tools, apps, spreadsheets), giving everyone one set of metric definitions, plus caching and pre-aggregations.\n\n**Tinybird** is a managed analytics backend. Your data moves into their ClickHouse, you write SQL Pipes, and each Pipe becomes a hosted API endpoint with auth, rate limiting, and caching handled for you.\n\n**hypequery** is an open-source TypeScript library. It points at the ClickHouse you already run, generates types from your schema, and turns queries written in your repo into typed REST endpoints and React hooks.\n\nA platform, a service, and a library. Most of the decision falls out of which of those shapes fits your team.\n\n## What each one is actually for\n\n### Cube: many consumers, one set of definitions\n\nCube earns its complexity when the same metrics need to reach multiple consumers — Tableau over the SQL API, an embedded dashboard over REST, a data app over GraphQL — and you need \"revenue\" to mean the same thing in all of them. Pre-aggregations are the other headline: for expensive queries hit by many users, Cube's caching layer does real work.\n\nThe cost is that Cube is infrastructure. There's an API server and a cache store to run (or a Cube Cloud bill), a modelling layer in YAML/JS to maintain, and a query JSON format your app speaks instead of SQL or your own types.\n\n### Tinybird: zero ops, fastest path to a hosted API\n\nTinybird's whole pitch is speed without infrastructure. Data streams in, SQL Pipes transform it, and endpoints appear with tokens, rate limits, and caching already sorted. For a team with no ops capacity that needs a public-facing data API this quarter, it's genuinely hard to beat.\n\nThe costs are the ones managed platforms always have: your data lives in their ClickHouse, pricing scales with data processed and API calls, and the workflow is SQL-first — your TypeScript app consumes untyped HTTP endpoints and maintains its own interfaces.\n\n### hypequery: your ClickHouse, your repo, your types\n\nhypequery assumes the database part is handled — you're on ClickHouse Cloud or self-hosting — and the missing piece is everything between ClickHouse and your TypeScript code. Types are generated from the live schema (with correct runtime mappings — `UInt64` comes back as a string, and your types know it). Queries are code. Endpoints come from `@hypequery/serve` with validation and OpenAPI docs. Dashboards use typed React hooks on the same contracts.\n\nThe cost: you run your own ClickHouse (or pay ClickHouse Cloud to), and auth, caching, and rate limiting come from your existing API stack rather than a platform.\n\n## The table\n\n| | Cube | Tinybird | hypequery |\n|---|---|---|---|\n| Shape | Semantic layer platform | Managed analytics service | Open-source library |\n| Your data lives | In your databases | In Tinybird's ClickHouse | In your ClickHouse |\n| Queries defined in | YAML/JS data models | SQL Pipes in their platform | TypeScript in your repo |\n| TypeScript types | Hand-written for Cube responses | Hand-written for HTTP responses | Generated from your schema |\n| BI tools (Tableau etc.) | Yes — a core strength | Not the focus | No |\n| Caching / pre-aggs | Built in — a core strength | Built in | Bring your own / ClickHouse MVs |\n| Auth & rate limiting | Built in | Built in | Your existing API stack |\n| Ops burden | Run Cube (or pay Cube Cloud) | None | Run ClickHouse (or use Cloud) |\n| Pricing | OSS + Cube Cloud tiers | Data processed + API calls | Free, open source |\n| Databases | Many | ClickHouse (theirs) | ClickHouse (yours) |\n\n## How to actually decide\n\n**Do BI tools need to consume the same metrics as your app?** If yes, Cube — it's the only one of the three that treats Tableau and Metabase as first-class consumers.\n\n**Do you have zero ops capacity and no ClickHouse yet?** Tinybird. Adopting ClickHouse plus an API layer is exactly the work it exists to remove. (Compare honestly against ClickHouse Cloud + hypequery, which keeps ownership at the cost of slightly more assembly.)\n\n**Do you already run ClickHouse and build in TypeScript?** This is hypequery's home turf. A platform in the middle adds a second place your schema lives, a second pricing meter, and a second deploy pipeline — for capabilities your API stack may already have.\n\n**Is data residency non-negotiable?** Tinybird is out. Cube and hypequery both leave data where it is.\n\n**Is the team SQL-first rather than TypeScript-first?** Tinybird's Pipes workflow will feel natural. hypequery's value — compile-time types, code review, refactoring — mostly pays off for teams living in TypeScript.\n\n## Can you mix them?\n\nSometimes sensibly. Cube for the data team's BI consumers plus hypequery for product-facing features is a real pattern — they serve different audiences from the same ClickHouse. Tinybird combines less well, since it wants to be the database too.\n\n## Where to go from here\n\nThe pairwise pages go deeper: [hypequery vs Cube](/compare/hypequery-vs-cube) and [hypequery vs Tinybird](/compare/hypequery-vs-tinybird). If you're leaving one of them, the switching guides cover migration specifics: [Cube.js alternative](/cube-js-alternative), [Tinybird alternative](/tinybird-alternative).\n\nAnd if you want to skip straight to evidence: the [quick start](/docs/quick-start) gets you from your ClickHouse schema to a typed, served endpoint in a few minutes — which is usually enough to know whether the library approach fits."
  },
  "hypequery-vs-dbt": {
    "slug": "hypequery-vs-dbt",
    "title": "hypequery vs dbt: Transformation Layer or Application Layer?",
    "description": "dbt transforms data inside ClickHouse on a schedule. hypequery serves ClickHouse data to applications at request time. Here is how the two fit together — and when one replaces the other.",
    "seoTitle": "hypequery vs dbt for ClickHouse — Which Layer Do You Need?",
    "seoDescription": "dbt models data inside ClickHouse. hypequery gives TypeScript apps typed queries and APIs on top of it. Compare both and see when you need one, the other, or both.",
    "date": "2026-07-03",
    "tags": [],
    "content": "dbt and hypequery both sit on top of ClickHouse, both live in version control, and both exist so analytics logic doesn't end up as untracked SQL strings scattered across the codebase. That's about where the similarity ends — they run at different times, in different places, for different people.\n\ndbt is a **transformation layer**: it runs SQL on a schedule inside ClickHouse and materialises the results as tables and views. hypequery is an **application layer**: it runs typed queries from your TypeScript code at request time and exposes them as APIs and React hooks.\n\nTeams end up comparing them because both show up when you search \"analytics layer on ClickHouse.\" This page separates the two jobs so you can figure out which one your stack actually needs — or whether it's both.\n\n## What dbt does\n\ndbt compiles SQL `SELECT` statements into ClickHouse-compatible SQL, runs them via the [dbt-clickhouse adapter](https://github.com/ClickHouse/dbt-clickhouse), and materialises the results as tables or views. Its strengths:\n\n- Versioned, documented, testable SQL transformations\n- Dependency ordering between models and incremental loads\n- A workflow data teams already know from the warehouse world\n\nIts relationship with ClickHouse works, but it's not seamless: no transactions, append-optimised storage, and MergeTree engine semantics mean some dbt patterns that are clean on Postgres or Snowflake need extra care here.\n\nThe important part: dbt runs on a **schedule or trigger**. It's not in the request path. It can't answer a user's dashboard filter or a tenant-scoped API call — that was never the job.\n\n## What hypequery does\n\nhypequery handles the request path instead. Your application needs to query ClickHouse in response to user actions, with runtime parameters, and it needs the results typed correctly:\n\n- **Generated types** from your live ClickHouse schema — including the tables dbt materialises — with correct runtime mappings (`DateTime` → string, `UInt64` → string, `Nullable` → `T | null`)\n- **A composable query builder** for filters, date ranges, aggregations, and tenant scoping decided at runtime\n- **Typed REST endpoints** via `@hypequery/serve`, with input validation and generated OpenAPI docs\n- **React hooks** that consume the same typed contract in dashboards\n\n## The honest comparison\n\n| | dbt | hypequery |\n|---|---|---|\n| Job | Transform data inside ClickHouse | Query and serve data to applications |\n| Runs | On a schedule (`dbt run`) | At request time, in your app |\n| Language | SQL + Jinja | TypeScript |\n| Consumers | Analysts, BI tools, downstream models | Product code, APIs, React components |\n| Type safety | Data tests on model output | Compile-time types generated from schema |\n| Schema role | Defines and materialises tables | Introspects whatever exists |\n| In the request path | No | Yes |\n\n## Complementary more than competitive\n\nThe most common production setup uses both, with a clean handoff between them:\n\n1. Raw events land in ClickHouse\n2. dbt models them into clean analytics tables on a schedule\n3. hypequery introspects those tables, generates types, and serves them to the application as typed queries and endpoints\n\nBecause hypequery generates types from the live schema, a dbt model change followed by a re-run of `generate` immediately surfaces any breakage in application code — at compile time, not in production. That's exactly the drift problem teams run into when the two layers are wired together by hand.\n\nFor a deeper treatment of where the transformation boundary should sit, read [TypeScript vs dbt for ClickHouse](/blog/typescript-vs-dbt-clickhouse).\n\n## When dbt alone is enough\n\n- Your ClickHouse consumers are analysts and BI tools, not application code\n- Nothing queries ClickHouse at request time\n- Your team is SQL-first and the output of analytics is reports, not product features\n\n## When hypequery alone is enough\n\n- Your transformations are simple enough for ClickHouse materialized views to handle\n- The main consumer of ClickHouse is your TypeScript application\n- Maintaining a separate transformation project would be overhead for a handful of tables that exist only to feed application APIs\n\n## When you want both\n\n- Raw event volumes need real modelling before they are queryable\n- A data team owns transformation logic while product engineers own application features\n- You want typed, compile-time-checked application access to the tables dbt produces\n\n## Getting started with hypequery\n\nThe [quick start](/docs/quick-start) covers pointing hypequery at your ClickHouse — including dbt-materialised tables — generating types, and serving your first typed endpoint. The [ClickHouse TypeScript](/clickhouse-typescript) guide covers the broader workflow."
  },
  "hypequery-vs-moose": {
    "slug": "hypequery-vs-moose",
    "title": "hypequery vs Moose (MooseStack): Framework or Library for ClickHouse?",
    "description": "Moose is a full-stack framework for analytical backends. hypequery is a typed query layer for the ClickHouse you already run. Here is how to choose between them.",
    "seoTitle": "hypequery vs Moose (MooseStack) — ClickHouse TypeScript Comparison",
    "seoDescription": "Moose (MooseStack) manages ClickHouse schema, streaming, and workflows as a framework. hypequery adds typed queries and APIs to your existing ClickHouse. Compare both.",
    "date": "2026-07-03",
    "tags": [],
    "content": "Moose (also called MooseStack, from 514 Labs) and hypequery are the two most direct answers to the same question: how should a TypeScript team build on ClickHouse without hand-writing types and boilerplate?\n\nThey disagree on one fundamental point, and it's worth getting clear on it before anything else. Moose is a **framework** — it wants to define your ClickHouse schema in code, run your local dev infrastructure, and manage streaming and orchestration around the database. hypequery is a **library** — it treats the ClickHouse you already run as the source of truth and adds a typed query and serving layer inside your existing application.\n\nAlmost every tradeoff below falls out of that one difference.\n\n## What Moose does\n\nMooseStack is an open-source developer framework for real-time analytical backends in TypeScript or Python. It's organised into modules:\n\n- **Moose OLAP** — declare ClickHouse tables, materialized views, and migrations as typed code. Schema lives in your repo and is pushed to ClickHouse, in the same direction Drizzle or Prisma push schema to Postgres.\n- **Moose Streaming** — ingestion buffers and streaming transformations backed by Kafka or Redpanda.\n- **Moose Workflows** — ETL pipelines and scheduled tasks backed by Temporal.\n- **Moose APIs** — typed ingestion and query endpoints served from your declared models.\n- **Moose Dev** — a local dev server that spins up the whole stack with hot reload, plus an MCP interface so AI agents can operate the dev environment.\n\n514 Labs also runs Boreal, a managed hosting platform that deploys MooseStack projects with preview environments and managed migrations.\n\nIf you're starting an analytical backend from scratch — ingestion, storage, transformation, APIs, all of it — Moose gives you one coherent, code-first way to build the whole thing.\n\n## What hypequery does\n\nhypequery starts from the opposite assumption: ClickHouse already exists in your stack, with its own ingestion pipelines and its own migration story already sorted. What's missing is the typed layer between ClickHouse and your TypeScript application.\n\n- **Schema generation** — introspect your live ClickHouse database and generate TypeScript types that match real runtime behaviour (`DateTime` as string, `UInt64` as string, `Nullable` as `T | null`).\n- **Query builder** — composable, fully typed analytics queries with ClickHouse-aware helpers and raw SQL escape hatches.\n- **Typed serving** — `@hypequery/serve` turns query definitions into REST endpoints with input validation, typed responses, and generated OpenAPI docs.\n- **React hooks** — consume the same typed query contracts from dashboards and product UI.\n\nThere's no dev runtime, no project scaffold, and no infrastructure footprint. It's an npm dependency in the app you already have.\n\n## The core difference: who owns the schema\n\nMoose is code-first: you declare tables in TypeScript and Moose migrates ClickHouse to match. That's powerful when the analytical backend is greenfield and the Moose project *is* the system of record for schema.\n\nhypequery is database-first: however your ClickHouse schema is managed — SQL migrations, Terraform, dbt, a data team with strong opinions — hypequery introspects it and generates types from what actually exists. That fits teams where ClickHouse predates the application layer, or where schema ownership sits outside the TypeScript codebase.\n\nNeither direction is wrong. The question is whether your TypeScript project should own ClickHouse or consume it.\n\n## The honest comparison\n\n| | Moose (MooseStack) | hypequery |\n|---|---|---|\n| Model | Framework — owns schema, dev runtime, infra lifecycle | Library — added to an existing app |\n| Schema | Declared in code, migrated to ClickHouse | Introspected from live ClickHouse |\n| Streaming ingest | Built in (Kafka/Redpanda) | Not included — bring your own pipeline |\n| Orchestration | Built in (Temporal) | Not included |\n| Typed queries | Yes | Yes |\n| Typed API endpoints | Yes (ingest + query APIs) | Yes (`@hypequery/serve` with OpenAPI) |\n| React integration | Bring your own | Typed hooks on the same query contract |\n| Local dev | `moose dev` runtime spins up the stack | Nothing extra — it is just your app |\n| Languages | TypeScript and Python | TypeScript |\n| Adoption cost | New project structure and runtime | `npm install` into existing code |\n| Open source | Yes (managed hosting via Boreal) | Yes |\n\n## When to choose Moose\n\n- You're building an analytical backend **from scratch** and want one framework for ingestion, schema, transformation, and APIs\n- You want streaming ingestion (Redpanda/Kafka) and workflow orchestration (Temporal) managed by the same tool that manages your tables\n- You want schema migrations owned by your TypeScript (or Python) codebase\n- The idea of an agent-operable dev runtime appeals to you, and you're fine adopting Moose's project structure\n- A managed deployment path (Boreal) is attractive\n\n## When to choose hypequery\n\n- ClickHouse **already exists** in your stack, with its own ingestion and migration story\n- You want typed queries, typed REST endpoints, and React hooks inside an application you already have\n- You don't want a framework runtime, a project scaffold, or new infrastructure dependencies in your dev loop\n- Schema ownership sits with a data team or another tool, and your TypeScript layer should follow that, not fight it\n- You want the smallest possible step from \"untyped ClickHouse calls\" to \"typed analytics layer\"\n\n## Can you use them together?\n\nNot really, and it's worth being direct about that. Both tools want to be the typed query and API layer between ClickHouse and your application, so running both just means two competing sources of truth for the same job. If Moose manages your ClickHouse, use Moose's query APIs. If ClickHouse is managed elsewhere, hypequery is the lighter, more natural fit.\n\n## Getting started with hypequery\n\nIf the library model fits, the [quick start](/docs/quick-start) takes you from schema introspection to a typed query and a served endpoint in a few minutes. For the broader picture of the workflow, read the [ClickHouse TypeScript](/clickhouse-typescript) guide, or see how the [query builder](/clickhouse-query-builder) handles filters, joins, and aggregations with generated types."
  },
  "hypequery-vs-propel": {
    "slug": "hypequery-vs-propel",
    "title": "hypequery vs Propel: Platform APIs or a Typed Layer in Your Own Repo?",
    "description": "Propel sells managed analytics APIs and embeddable UI components over ClickHouse. hypequery is the code-first route to the same outcome. Here's how to pick.",
    "seoTitle": "hypequery vs Propel — ClickHouse Analytics API Comparison",
    "seoDescription": "Propel is a serverless ClickHouse platform with GraphQL APIs and embedded analytics components. hypequery is an open-source TypeScript layer for your own ClickHouse. Compare both.",
    "date": "2026-07-03",
    "tags": [],
    "content": "Propel and hypequery are both trying to close the same gap: ClickHouse is fast, but there's a lot of work between \"the data is in ClickHouse\" and \"our product has analytics.\"\n\nPropel closes it with a platform. You connect data — Kafka, S3, Postgres, Snowflake, or an existing ClickHouse — and Propel gives you managed query APIs, a semantic layer for metric definitions, multi-tenant access policies, and embeddable React components for dashboards.\n\nhypequery closes it with code. You point it at the ClickHouse you already run, it generates TypeScript types from your schema, and you write queries and serve them as typed endpoints from your own application.\n\nSame destination, very different relationship with your stack.\n\n## What Propel does well\n\nPropel has clearly optimised for time-to-shipped-dashboard, and it shows:\n\n- **Managed serving.** GraphQL and SQL APIs with auto-scaling, without building or operating an API layer yourself.\n- **Embeddable UI components.** Prebuilt React components for charts and dashboards that sit directly on Propel's APIs. If the job is \"put a usage dashboard in our SaaS product this month,\" this is the fastest route on this page.\n- **A semantic layer.** Metrics defined once, queried from anywhere, with multi-tenant access policies enforced at the platform level.\n- **Flexible data plumbing.** Ingest from Kafka, S3, Snowflake, or Postgres — or connect your existing self-hosted ClickHouse or ClickHouse Cloud, in which case your data stays put.\n\nThat last point matters: unlike some managed platforms, Propel doesn't force ingestion. The bring-your-own-ClickHouse option removes the data residency objection.\n\n## Where the platform model costs you\n\n**The serving layer isn't yours.** Your queries run through Propel's APIs, your metric definitions live in Propel's semantic layer, and your dashboards render Propel's components. That's the point — but it also means your analytics surface is coupled to a vendor's platform, roadmap, and uptime.\n\n**Types come from their schema, not yours.** You can codegen TypeScript from Propel's GraphQL API, which is better than nothing — but the types describe Propel's query interface, not your ClickHouse tables. There's still a translation layer between your data model and your application code.\n\n**Usage-based pricing.** Storage and query costs scale with the product's success. Predictable for small workloads; a line item worth watching for high-traffic embedded analytics.\n\n**Another definition of your metrics.** If your team already defines metrics in code, adding a platform-level semantic layer means one more place where \"active users\" has to mean the same thing.\n\n## What hypequery does instead\n\nhypequery assumes the thing you want to own is exactly the thing Propel manages for you:\n\n1. Run `generate` against your ClickHouse — types are created from your live schema, with ClickHouse-correct runtime mappings\n2. Write queries in TypeScript with the composable query builder; runtime parameters like date ranges and tenant IDs are just function arguments\n3. Serve them as REST endpoints with validation and OpenAPI docs via `@hypequery/serve`\n4. Build dashboards with your own components, consuming the same typed contracts through React hooks\n\nEverything is open source, everything lives in your repo, and the only infrastructure is the ClickHouse you were already running.\n\n## The honest comparison\n\n| | Propel | hypequery |\n|---|---|---|\n| Model | Serverless platform | Open-source library |\n| Serving | Managed GraphQL and SQL APIs | Typed REST endpoints in your app |\n| UI | Embeddable React components included | Bring your own components, typed hooks provided |\n| Types | Codegen from Propel's GraphQL schema | Generated from your ClickHouse schema |\n| Semantic layer | Platform-level metric definitions | Metrics as TypeScript code in your repo |\n| Multi-tenancy | Platform access policies | Your auth context, injected per request |\n| Data location | Managed, or bring your own ClickHouse | Always your own ClickHouse |\n| Pricing | Usage-based | Free — you pay for your infra |\n\n## When to choose Propel\n\n- You need customer-facing dashboards live in weeks, and prebuilt components get you there\n- Nobody on the team wants to build or run an API layer, even a thin one\n- Platform-enforced multi-tenant access policies fit your compliance story\n- Usage-based pricing is acceptable for your traffic profile\n\n## When to choose hypequery\n\n- You want the analytics layer in your own repo, reviewed and shipped like the rest of your code\n- Response types should come from your actual ClickHouse schema, not a platform's API\n- You already have components, auth, and an API stack — you need the typed ClickHouse layer, not a second platform\n- You'd rather pay for infrastructure than per query\n\n## Getting started\n\nThe [quick start](/docs/quick-start) goes from schema introspection to a served, typed endpoint in a few minutes. If embedded analytics is the use case you're weighing, the [ClickHouse SaaS analytics](/clickhouse-saas-analytics) guide covers tenant isolation patterns, and [ClickHouse React](/clickhouse-react) shows the hooks side of the same contract."
  },
  "hypequery-vs-typeorm": {
    "slug": "hypequery-vs-typeorm",
    "title": "hypequery vs TypeORM for ClickHouse",
    "description": "TypeORM has no ClickHouse driver and no real workaround — its entity model doesn't fit columnar storage. hypequery is the ClickHouse-native TypeScript layer, and the practical answer is to run both side by side.",
    "seoTitle": "hypequery vs TypeORM for ClickHouse",
    "seoDescription": "TypeORM has no ClickHouse driver. hypequery is the ClickHouse-native TypeScript layer. See why they coexist rather than compete, with a side-by-side example.",
    "date": "2026-07-18",
    "tags": [],
    "content": "## Short answer\n\nTypeORM does not support ClickHouse. There's no driver, and unlike Prisma or Drizzle there isn't even an experimental MySQL-port trick worth trying. That's not a gap TypeORM will close soon, and it's not a bug — TypeORM is an entity ORM built for transactional row stores, and ClickHouse is a columnar, append-only analytics database. The two models genuinely don't fit.\n\nSo this isn't really a \"which one wins\" comparison. TypeORM is fine at its actual job. hypequery is the [ClickHouse-native TypeScript layer](/clickhouse-typescript) for the analytics side. The realistic outcome is that you run **both**: TypeORM stays on Postgres or MySQL, and hypequery handles ClickHouse. This page explains why the ORM model doesn't map onto ClickHouse, and shows the two working side by side in one service.\n\nIf you just want to see hypequery run, the [quick start](/docs/quick-start) goes from a live ClickHouse schema to a typed query in a few minutes.\n\n## What TypeORM is good at\n\nTypeORM is a mature, well-liked ORM for Node.js, and it earns its place in a lot of Nest and Express apps. Give it credit for what it does well:\n\n- **Entities as classes.** You decorate a `User` class, and TypeORM handles the mapping to a `users` table, relations to other entities, and lazy loading.\n- **Transactions.** Wrap a set of writes in a transaction and get atomic commit/rollback — exactly what you want when creating an order and its line items together.\n- **Migrations.** Schema changes are generated and versioned from your entity definitions.\n- **The identity map.** Within a unit of work, the same database row is the same object in memory, which keeps mutation sane.\n- **Relational integrity.** Foreign keys, cascades, and joins across entities are first-class.\n\nEvery one of those features assumes a database that does row-level updates, enforces constraints, and runs transactions. That's Postgres, MySQL, SQLite, SQL Server, and friends. It is precisely *not* ClickHouse.\n\n## Why the ORM model doesn't map onto ClickHouse\n\nClickHouse is columnar and optimized for reading huge ranges of rows and aggregating them fast. It makes deliberate tradeoffs that break the assumptions an ORM is built on:\n\n- **No real transactions.** There's no `BEGIN … COMMIT` you'd wrap a unit of work in. TypeORM's transaction API has nothing to bind to.\n- **Append-optimized, mutation-averse.** Updates and deletes exist but are asynchronous background operations (`ALTER TABLE … UPDATE`), not the row-level UPDATE an identity map assumes. The whole \"load an entity, mutate it, flush it back\" loop doesn't apply.\n- **No enforced foreign keys.** ClickHouse doesn't enforce referential integrity. The relation graph an ORM manages simply isn't there to manage.\n- **Deduplication is a merge-time concern.** With `ReplacingMergeTree` you dedupe at merge time (eventually) or force it at query time with `FINAL` — there's no unique constraint doing it for you on write.\n- **Engine choice is the schema.** `MergeTree`, `ReplacingMergeTree`, `SummingMergeTree`, sorting keys, partitioning — these are the meaningful decisions in ClickHouse, and TypeORM's decorator model has no vocabulary for them.\n\nThis is the same reason [there's no Prisma for ClickHouse](/blog/does-clickhouse-have-an-orm) and no Drizzle driver for it either. ORMs model row-level transactional entities; ClickHouse is aggregation-first. It's a category mismatch, not a missing feature. We go deeper on that distinction in [query builder vs ORM for ClickHouse](/blog/query-builder-vs-orm-clickhouse).\n\n## What hypequery does instead\n\nhypequery doesn't try to be an ORM for ClickHouse — it's a typed query builder and serving layer that leans into the columnar model instead of fighting it:\n\n- **Generated types.** Run `hypequery generate` and get TypeScript types from your live ClickHouse schema, with the runtime realities baked in: `UInt64` comes back as a string, `DateTime` as a string, `Nullable(T)` as `T | null`.\n- **A composable query builder.** Filters, aggregations, joins, time grains, and tenant scoping, all typed against your schema.\n- **Typed serving.** `@hypequery/serve` turns a query into a validated REST endpoint with generated OpenAPI docs.\n- **React hooks.** `@hypequery/react` consumes the same typed contract in dashboards.\n\nHere's an analytics query — revenue per day for one tenant — that has no clean TypeORM equivalent:\n\n```ts\nimport { createQueryBuilder } from '@hypequery/clickhouse';\nimport type { IntrospectedSchema } from './generated/schema.js';\n\nconst db = createQueryBuilder<IntrospectedSchema>({\n  url: process.env.CLICKHOUSE_URL!,\n  username: process.env.CLICKHOUSE_USERNAME!,\n  password: process.env.CLICKHOUSE_PASSWORD,\n  database: process.env.CLICKHOUSE_DATABASE!,\n});\n\nconst revenueByDay = await db\n  .table('orders')\n  .where('tenant_id', 'eq', tenantId)\n  .groupByTimeInterval('created_at', '1 day')\n  .sum('total', 'revenue')\n  .orderBy('created_at', 'ASC')\n  .execute();\n```\n\nThat's a `GROUP BY toStartOfDay(created_at)` aggregation — the kind of query ClickHouse exists to run and an entity ORM was never meant to express.\n\n## The honest comparison\n\n| | TypeORM | hypequery |\n|---|---|---|\n| Built for | OLTP row stores (Postgres, MySQL, SQLite) | ClickHouse analytics |\n| ClickHouse support | None — no driver, no workaround | Native |\n| Core abstraction | Entities, relations, identity map | Typed queries, measures, dimensions |\n| Transactions | Yes — a core feature | No — ClickHouse has no unit-of-work model |\n| Schema source | Decorated entity classes → migrations | Introspected from live ClickHouse |\n| Type mapping | OLTP column types | ClickHouse-correct runtime types |\n| Serving layer | None (it's a data-access ORM) | REST + OpenAPI + React hooks |\n\n## Coexistence: TypeORM and hypequery in one service\n\nThe common real-world shape: transactional data lives in Postgres or MySQL (users, orders, subscriptions) and event/analytics data lives in ClickHouse (usage events, aggregated metrics). TypeORM owns the first; hypequery owns the second. They don't overlap, so they coexist cleanly in a single Nest or Express app.\n\n```ts\n// TypeORM — transactional writes against Postgres\nconst orderRepo = dataSource.getRepository(Order);\nawait dataSource.transaction(async (manager) => {\n  const order = manager.create(Order, { tenantId, total, status: 'paid' });\n  await manager.save(order);\n});\n\n// hypequery — analytics reads against ClickHouse\nconst topRegions = await db\n  .table('orders')\n  .where('tenant_id', 'eq', tenantId)\n  .select(['region'])\n  .sum('total', 'revenue')\n  .groupBy('region')\n  .orderBy('revenue', 'DESC')\n  .limit(10)\n  .execute();\n```\n\nEach tool does what it's built for. TypeORM guarantees the write is atomic; hypequery aggregates millions of rows in the read path with types that match what ClickHouse actually returns. Neither is pretending to be the other.\n\nIf you want to expose that analytics read as a governed HTTP route, [`@hypequery/serve`](/clickhouse-api) wraps it with zod validation and OpenAPI docs — so the same query is usable in-process and over HTTP without hand-writing response types.\n\n## When TypeORM is the right call\n\nDon't reach for hypequery where TypeORM belongs. Stay on TypeORM when:\n\n- Your data is transactional entities with relations and constraints.\n- You need atomic multi-row writes.\n- You're on Postgres, MySQL, SQLite, or another engine TypeORM supports.\n- The work is CRUD and business logic, not analytical aggregation.\n\nAnd reach for hypequery when the data lives in ClickHouse and the queries are aggregations, time series, and tenant-scoped analytics served to an app.\n\n## Where to go from here\n\n- [TypeORM and ClickHouse](/typeorm-clickhouse) — the fuller version of why there's no driver, and what to use instead.\n- [Does ClickHouse have an ORM?](/blog/does-clickhouse-have-an-orm) — the category mismatch, spelled out.\n- [Query builder vs ORM for ClickHouse](/blog/query-builder-vs-orm-clickhouse) — why the query-builder shape fits columnar analytics.\n- [ClickHouse ORM](/clickhouse-orm) — the pillar page on the whole \"ORM for ClickHouse\" question.\n\nWhen you're ready, the [quick start](/docs/quick-start) takes you from schema introspection to a typed, served endpoint — the fastest way to see whether the library approach fits the ClickHouse side of your stack."
  },
  "hypequery-vs-metabase": {
    "slug": "hypequery-vs-metabase",
    "title": "hypequery vs Metabase for Embedded ClickHouse Analytics",
    "description": "Metabase is a BI tool with a ClickHouse connector and embedding features — the fast path for internal dashboards. hypequery is the code-first route for customer-facing product analytics you own. Here's how to choose.",
    "seoTitle": "hypequery vs Metabase for ClickHouse Analytics",
    "seoDescription": "Metabase embeds BI dashboards over ClickHouse; hypequery is a code-first embedded analytics library. Compare the two for customer-facing product analytics.",
    "date": "2026-07-18",
    "tags": [],
    "content": "Short answer: if you want a chart in front of people this week, Metabase is the faster path — it's a mature BI tool with a native ClickHouse connector and real embedding features. If you're building customer-facing product analytics that ships inside your app, with type-safe queries, per-tenant rules enforced in your own auth stack, and a UI that matches your product, that's code-first territory, and hypequery is built for it.\n\nThese aren't the same category, and pretending they are does nobody any favors. Metabase is a BI application. hypequery is a TypeScript library you point at the ClickHouse you already run. This page is about which shape fits the job, not a feature war.\n\n## What Metabase does well\n\nMetabase is genuinely good, and for a large set of analytics jobs it's the right answer.\n\n- **Fast time-to-chart.** Connect ClickHouse via the official connector, build a \"question\" in the UI, and you have a dashboard. No code, no deploy, no schema generation step.\n- **Non-engineers can self-serve.** Analysts, PMs, and support staff can slice data, save questions, and build dashboards without waiting on the eng team. That's the whole point of a BI tool, and it's a real capability hypequery does not have.\n- **Embedding exists.** Static iframe embeds are free. Interactive embedding — with SSO and row-level sandboxing so each customer sees only their data — is available on paid tiers, along with an embedding SDK for more control over the frame.\n- **Batteries included.** Alerts, scheduled emails, a permissions model, a query editor with a visual and SQL mode. You don't build any of this.\n\nIf the job is \"put an internal revenue dashboard in front of the leadership team,\" or \"let ops explore event data without writing SQL,\" Metabase wins on almost every axis. Reaching for a library there would be over-engineering.\n\n## Where the BI-embed model gets expensive\n\nThe friction shows up when the dashboard is *your product*, shipped to your customers, rather than an internal tool.\n\n**The UI is Metabase's, not yours.** Embedded Metabase looks like embedded Metabase. You can theme it, and the SDK gives you more room, but you're styling someone else's charts inside a frame rather than composing your own React components. For a polished, customer-facing surface that has to match your product's design system, that ceiling gets real.\n\n**Per-tenant governance lives in Metabase.** Interactive embedding with sandboxing does enforce row-level rules — but those rules live in the BI tool's permission model, configured separately from the authentication and authorization your application already has. For a multi-tenant SaaS product, that's a second source of truth for \"who can see what,\" and it's a paid-tier feature. When your app already knows the tenant from the request, injecting that scope in your own code is often simpler and easier to audit.\n\n**Queries aren't code.** Questions authored in the Metabase UI don't live in your repo, don't go through code review, and aren't covered by your CI. There are no compile-time types tying a chart to your ClickHouse schema, so a column rename surfaces as a broken dashboard at runtime rather than a type error at build time.\n\n**Pricing scales with embedding.** The free tier covers internal use and static embeds. The moment you need interactive embedding, SSO, and sandboxing for customer-facing analytics, you're on a paid plan — a line item that grows with the product.\n\nNone of these are bugs. They're the natural shape of a BI tool. They just point at a different set of jobs than product analytics.\n\n## What hypequery does instead\n\nhypequery assumes you own the ClickHouse and you're building the analytics surface yourself, in TypeScript.\n\n1. Run `hypequery generate` against your live ClickHouse. It introspects the schema and produces TypeScript types with correct runtime mappings — `DateTime` comes back as a string, `UInt64` as a string, `Nullable(T)` as `T | null`.\n2. Write queries with a fully typed builder. Runtime parameters — date ranges, tenant IDs, filters — are just function arguments.\n3. Serve them as typed REST endpoints with input validation and generated OpenAPI docs via `@hypequery/serve`.\n4. Build the dashboard from your own components, consuming the same typed contract through `@hypequery/react` hooks.\n\nA tenant-scoped query looks like ordinary application code:\n\n```ts\nimport { createQueryBuilder } from '@hypequery/clickhouse';\nimport type { IntrospectedSchema } from './generated/schema.js';\n\nconst db = createQueryBuilder<IntrospectedSchema>({\n  url: process.env.CLICKHOUSE_URL!,\n  username: process.env.CLICKHOUSE_USERNAME!,\n  password: process.env.CLICKHOUSE_PASSWORD,\n  database: process.env.CLICKHOUSE_DATABASE!,\n});\n\nconst revenueByDay = (tenantId: string) =>\n  db\n    .table('orders')\n    .where('tenant_id', 'eq', tenantId)\n    .groupByTimeInterval('created_at', '1 day')\n    .sum('total', 'revenue')\n    .orderBy('created_at', 'ASC')\n    .execute();\n```\n\nThe `tenant_id` filter comes from your request context — the same auth you already trust for the rest of the app. There's no second permission system to keep in sync. And because the return type is generated from the real schema, renaming `total` in ClickHouse and re-running `generate` turns every stale query into a compile error, not a broken customer dashboard.\n\nServing it is a small step from there:\n\n```ts\nimport { initServe } from '@hypequery/serve';\nimport { z } from 'zod';\nimport { db } from './client.js';\n\nconst { query, serve } = initServe({ context: () => ({ db }), basePath: '/api/analytics' });\n\nconst revenue = query({\n  description: 'Daily revenue for the current tenant',\n  input: z.object({ tenantId: z.string() }),\n  query: ({ ctx, input }) =>\n    ctx.db\n      .table('orders')\n      .where('tenant_id', 'eq', input.tenantId)\n      .groupByTimeInterval('created_at', '1 day')\n      .sum('total', 'revenue')\n      .execute(),\n});\n\nexport const api = serve({ queries: { revenue } });\n```\n\nThat endpoint is validated, documented, and consumed by typed React hooks in your own charts. The trade you're making: you build the UI and wire the auth, instead of getting them from a BI tool.\n\n## The honest comparison\n\n| | Metabase | hypequery |\n|---|---|---|\n| Category | BI tool with a ClickHouse connector | Code-first embedded analytics library |\n| Best for | Internal dashboards, self-serve exploration | Customer-facing product analytics |\n| Who authors queries | Anyone, in the UI | Engineers, in TypeScript |\n| The UI | Metabase charts, embedded via iframe / SDK | Your own React components |\n| Type safety | None — queries live in the BI UI | Generated from your ClickHouse schema |\n| Multi-tenancy | Sandboxing / row-level rules (paid tiers) | Tenant scope from your auth, per request |\n| In your repo / code review | No | Yes |\n| Ops | Run Metabase (or Metabase Cloud) | Add npm packages to your existing app |\n| Pricing | Free tier; interactive embedding is paid | Open source — you pay for your infra |\n\n## How to actually decide\n\n**Is the audience internal?** Metabase, almost every time. Self-serve exploration for non-engineers is exactly what it's for, and building that with a library would be wasted effort.\n\n**Is the dashboard a feature of your product, shipped to customers?** This is hypequery's home turf. When the analytics surface has to match your design system, respect your existing auth, and ship through your normal review-and-deploy pipeline, owning it in code pays off. The [ClickHouse dashboard](/clickhouse-dashboard) guide walks through building one, and [ClickHouse React](/clickhouse-react) covers the hooks side of the contract.\n\n**Do per-tenant rules already live in your app?** If your backend already knows the tenant from the request, enforcing that scope in your own query code keeps one source of truth. Our write-up on [multi-tenant embedded analytics](/blog/embedded-analytics-clickhouse-multi-tenant-dashboards) covers the isolation patterns, and [ClickHouse SaaS analytics](/clickhouse-saas-analytics) covers the broader tenant story.\n\n**Do non-engineers need to build their own charts?** Metabase — that's a job hypequery deliberately doesn't do.\n\n## Can you use both?\n\nYes, and it's a common, sensible split. Metabase for internal BI and ad-hoc exploration; hypequery for the customer-facing analytics baked into the product. Both point at the same ClickHouse, and they serve genuinely different audiences from it. Running both isn't redundant the way two application layers would be — one is for your team, one is for your users.\n\n## Getting started with hypequery\n\nIf the customer-facing, code-first side is the one you're weighing, the [quick start](/docs/quick-start) takes you from schema introspection to a typed, served endpoint in a few minutes — enough to see whether owning the analytics layer in your repo fits how your team ships. From there, [ClickHouse React](/clickhouse-react) shows the dashboard hooks, and the [multi-tenant analytics write-up](/blog/embedded-analytics-clickhouse-multi-tenant-dashboards) covers per-tenant governance in your own stack."
  },
  "hypequery-vs-clickhouse-http": {
    "slug": "hypequery-vs-clickhouse-http",
    "title": "hypequery vs the ClickHouse HTTP Interface",
    "description": "The ClickHouse HTTP interface is a great way to run queries from the shell. hypequery uses the same protocol underneath and adds types, bound parameters, and reuse for application code. Here's when to reach for which.",
    "seoTitle": "hypequery vs ClickHouse HTTP Interface",
    "seoDescription": "curl over ClickHouse HTTP is perfect for scripts and health checks. hypequery uses the same protocol and adds types, safe parameters, and reuse for app code.",
    "date": "2026-07-18",
    "tags": [],
    "content": "If you run ClickHouse, you already know the HTTP interface. It's the first thing that works: point curl at port 8123, pass a query, ask for `FORMAT JSON`, and you get data back.\n\n```bash\ncurl 'http://localhost:8123/?query=SELECT%20region,%20sum(total)%20FROM%20orders%20GROUP%20BY%20region%20FORMAT%20JSON'\n```\n\n**Short answer:** don't replace this for what it's good at. The HTTP interface is simple, fast, and dependency-free, and it's the right tool for scripts, health checks, and one-off queries. What breaks down is using it directly from application code — where the same query needs runtime parameters, correct types, and a definition you can reuse. hypequery speaks the *same* HTTP protocol underneath (it's built on `@clickhouse/client`), so this isn't a protocol argument. It's about what sits on top of the protocol.\n\n## What the HTTP interface does well\n\nThe [ClickHouse HTTP interface](/clickhouse-api) is genuinely excellent, and it's worth being specific about why:\n\n- **Zero dependencies.** Any language with an HTTP client can talk to it. No driver, no ORM, no build step.\n- **Fast and direct.** You're talking to the server with nothing in the middle. Latency is the network plus the query.\n- **Great for automation.** Health checks (`SELECT 1`), CI probes, cron jobs, quick data pulls — the HTTP endpoint is the obvious right answer for all of them.\n- **Flexible output.** `FORMAT JSON`, `JSONEachRow`, `CSV`, `TSV`, `Pretty` — one endpoint, many shapes.\n\nIf your task is \"check the cluster is up\" or \"pull yesterday's totals into a spreadsheet,\" stop reading. curl is the tool.\n\n## Where curl-over-HTTP stops scaling\n\nThe trouble starts when the HTTP interface moves from your terminal into your backend, and the query stops being a fixed string. Four things go wrong, and they compound.\n\n### 1. String-concatenated SQL\n\nA real endpoint takes input. A region filter, a date range, a tenant ID. With the HTTP interface, that means building the SQL string yourself:\n\n```ts\nconst region = req.query.region; // user input\nconst sql = `SELECT sum(total) FROM orders WHERE region = '${region}' FORMAT JSON`;\nawait fetch(`http://localhost:8123/?query=${encodeURIComponent(sql)}`);\n```\n\nThat interpolation is a SQL injection hole, and even when it isn't malicious it's an escaping bug waiting to happen — a region name with an apostrophe breaks the query. ClickHouse does support server-side bound parameters over HTTP (`param_region`), but wiring them by hand across every endpoint is exactly the boilerplate that gets skipped under deadline.\n\n### 2. No types on the response\n\n`FORMAT JSON` gives you `{ data: [...], meta: [...], rows: N }`. Every field is whatever JSON says it is, and you annotate it by hand:\n\n```ts\nconst body = await res.json();\nconst rows = body.data as { region: string; total: number }[]; // hope this is right\n```\n\nThat cast is a promise you're making to the compiler with nothing backing it. And it's frequently wrong in ClickHouse-specific ways: `UInt64` comes back as a **string**, not a number; `DateTime` is a string; `Nullable(T)` can be `null`. Hand-written types drift from the real schema, and the mismatch surfaces at runtime.\n\n### 3. No reuse\n\nThe \"revenue by region\" query is needed in the dashboard endpoint, the export job, and the internal admin view. Over raw HTTP, that's the same SQL string pasted in three places. When the definition of revenue changes, you change it three times and hope you found them all. This is the exact failure mode covered in [stop writing the same query three times](/blog/stop-writing-the-same-query-three-times).\n\n### 4. DIY governance on anything exposed\n\nThe moment an HTTP-backed query sits behind a route users can hit, you own validation, auth, and rate limiting yourself. There's no schema on the input, no place to enforce a tenant boundary, no generated API contract for consumers. All of it is hand-rolled per endpoint.\n\n## Same protocol, typed contract on top\n\nhypequery doesn't bypass the HTTP interface — it *uses* it, through `@clickhouse/client`, which is the official Node client that talks the same HTTP protocol to ClickHouse. What hypequery adds is the layer between that transport and your application.\n\nYou generate types from your live schema once:\n\n```bash\nnpx hypequery generate\n```\n\nThen the same \"revenue by region, filtered by input\" query looks like this:\n\n```ts\nimport { createQueryBuilder } from '@hypequery/clickhouse';\nimport type { IntrospectedSchema } from './generated/schema.js';\n\nconst db = createQueryBuilder<IntrospectedSchema>({\n  url: process.env.CLICKHOUSE_URL!,\n  username: process.env.CLICKHOUSE_USERNAME!,\n  password: process.env.CLICKHOUSE_PASSWORD,\n  database: process.env.CLICKHOUSE_DATABASE!,\n});\n\nconst rows = await db\n  .table('orders')\n  .select(['region'])\n  .sum('total', 'revenue')\n  .where('region', 'eq', region) // bound, not concatenated\n  .groupBy('region')\n  .execute();\n```\n\nThe `where` value is a bound parameter, not string interpolation, so injection and escaping aren't your problem. `rows` is typed from the generated schema, including the runtime realities — `revenue` is typed to match how ClickHouse actually returns the aggregate, and a `UInt64` column would come back as a string, correctly typed. The query is one definition you import wherever it's needed.\n\nAnd when that query needs to be an endpoint, `@hypequery/serve` gives you validation, auth hooks, and OpenAPI docs on the same definition — see [`/clickhouse-rest-api`](/clickhouse-rest-api) and [`/clickhouse-openapi`](/clickhouse-openapi):\n\n```ts\nimport { initServe } from '@hypequery/serve';\nimport { z } from 'zod';\nimport { db } from './client.js';\n\nconst { query, serve } = initServe({ context: () => ({ db }), basePath: '/api/analytics' });\n\nconst revenueByRegion = query({\n  description: 'Revenue for a single region',\n  input: z.object({ region: z.string() }),\n  query: ({ ctx, input }) =>\n    ctx.db\n      .table('orders')\n      .select(['region'])\n      .sum('total', 'revenue')\n      .where('region', 'eq', input.region)\n      .groupBy('region')\n      .execute(),\n});\n\nexport const api = serve({ queries: { revenueByRegion } });\n```\n\nThe input is validated by zod before any SQL runs, the response type flows through to callers, and the same contract is consumed by [React hooks](/clickhouse-react) on the dashboard.\n\n## The honest comparison\n\n| | ClickHouse HTTP interface | hypequery |\n|---|---|---|\n| Transport | HTTP to port 8123 | Same HTTP, via `@clickhouse/client` |\n| Best for | Scripts, health checks, one-offs | Application code at request time |\n| Parameters | You build the SQL string | Bound parameters via the builder |\n| Response types | Untyped JSON, hand-annotated | Generated from your schema |\n| Runtime mappings | You remember `UInt64` is a string | Encoded in the generated types |\n| Reuse | Copy the query string | One imported definition |\n| Exposed endpoints | DIY validation and auth | zod, auth hooks, OpenAPI via serve |\n| Dependencies | None | An npm dependency |\n\n## When the HTTP interface is still the right call\n\nBe clear-eyed about this: hypequery is a dependency in your application, and there are jobs where that's overhead you don't want.\n\n- **Health checks and probes.** `curl 'http://host:8123/?query=SELECT%201'` is perfect. Don't wrap it.\n- **Ad-hoc exploration.** Poking at data from a shell or a notebook — the HTTP endpoint is faster to reach for than any library.\n- **Non-TypeScript scripts.** A Python cron job or a Go health checker has no reason to adopt a TypeScript query layer.\n- **Bulk exports and inserts.** Streaming a large result or loading data is transport-level work; the client and the HTTP interface handle it directly.\n\nhypequery earns its place when queries live *in* your TypeScript application, take runtime input, need correct types, and get reused. It's not either/or — the same protocol runs underneath either way. For the deeper look at the client hypequery builds on, see [hypequery vs @clickhouse/client](/compare/hypequery-vs-clickhouse-client) and the [clickhouse-js](/clickhouse-js) guide.\n\n## Getting started\n\nThe [quick start](/docs/quick-start) takes you from your existing ClickHouse — the same server you've been curl-ing — to generated types and a served, typed endpoint in a few minutes. If you want more background first, [`/clickhouse-api`](/clickhouse-api) covers the interface options, and [`/clickhouse-rest-api`](/clickhouse-rest-api) covers putting a governed API in front of your queries."
  },
  "hypequery-vs-raw-sql": {
    "slug": "hypequery-vs-raw-sql",
    "title": "hypequery vs Raw SQL Strings in TypeScript",
    "description": "Raw SQL strings are the right call for one-off scripts and gnarly analytical queries. For the queries embedded in your app, hand-written SQL and interfaces drift from your schema. Here is where the line sits — and why hypequery keeps the raw-SQL escape hatch.",
    "seoTitle": "hypequery vs Raw SQL Strings in TypeScript",
    "seoDescription": "When to keep writing raw ClickHouse SQL strings and when a typed query builder pays off: schema drift, refactoring, injection risk, and escape hatches.",
    "date": "2026-07-18",
    "tags": [],
    "content": "## Short answer\n\nDo you even need a query builder? Sometimes not. If you're writing a one-off backfill script, a migration, or one genuinely gnarly analytical query — window functions, ASOF joins, three nested CTEs — a raw SQL string is the clearest thing you can write, and reaching for an abstraction just adds noise. hypequery agrees with this. It ships `selectExpr`, `rawAs`, and `withCTE` precisely so you can drop to raw SQL whenever the builder gets in the way. You never have to fight it.\n\nThe case for the builder isn't \"raw SQL is bad.\" It's about the *other* SQL — the queries that live inside your application, get reused across endpoints, filtered by user input, and refactored as the schema changes. That's where raw SQL strings plus hand-written result interfaces quietly rot: rename a column and nothing fails until runtime. This page is about that practice — SQL strings wherever they live, in template literals, `.sql` files, or any client — not about any one library. (For the narrower comparison with the official transport client, see [hypequery vs @clickhouse/client](/compare/hypequery-vs-clickhouse-client), which hypequery is actually built on.)\n\nIf you'd rather just see it run, the [quick start](/docs/quick-start) goes from a live ClickHouse schema to a typed query in a few minutes.\n\n## Where raw SQL is genuinely the right call\n\nBe honest about this up front, because it's where a lot of \"just use a library\" advice goes wrong:\n\n- **One-off scripts and backfills.** A script you run once and delete doesn't benefit from types. Write the SQL, run it, move on.\n- **Migrations and DDL.** `CREATE TABLE`, `ALTER`, engine and partition tuning — this is raw SQL territory and always will be.\n- **Genuinely gnarly analytics.** A query with several window functions, an ASOF JOIN, and correlated subqueries is often clearer as SQL than as any fluent chain. hypequery doesn't have an `asofJoin` method, and it's honest about that — ASOF JOIN is raw-SQL territory.\n\nIf that's the shape of your work, you may not need a builder at all. The builder earns its place when queries are *repeated and embedded*, not throwaway.\n\n## Where hand-written SQL strings start to hurt\n\nThe problems don't show up on day one. They show up three months later, in an app with fifty queries scattered across route handlers.\n\n**Schema drift is silent.** You rename `total` to `amount` in ClickHouse. Your hand-written string `SELECT sum(total) ...` still compiles, still type-checks against your hand-written `interface OrderRow`, and still passes review. It fails at runtime, in production, when ClickHouse rejects the unknown column. Nothing in your toolchain warned you.\n\n**There's no refactoring support.** Rename a column and you're doing a project-wide string search, hoping you catch every `.sql` file and every template literal. \"Find all references\" doesn't work on strings. Neither does rename-symbol.\n\n**Reuse becomes copy-paste.** The same 20-line revenue query gets pasted into three endpoints with slightly different `WHERE` clauses. Now a fix has to land in three places, and they drift apart over time. (This exact pattern is the subject of [stop writing the same query three times](/blog/stop-writing-the-same-query-three-times).)\n\n**Interpolation is an injection risk.** The moment a raw string stops being static and starts including a runtime value, you're one careless template literal away from an injection bug:\n\n```ts\n// The bug that raw strings invite\nconst rows = await client.query({\n  query: `SELECT * FROM orders WHERE region = '${region}'`,\n});\n// region = \"x' OR '1'='1\" and you've got a problem\n```\n\nParameterization fixes this — but it's opt-in, and \"remember to parameterize every value, forever, across the whole team\" is not a safety model. It's a hope.\n\n## Before and after\n\nHere's a realistic analytics query — daily revenue by region for one tenant — written both ways. First, the raw-string version with a hand-written interface:\n\n```ts\nimport { createClient } from '@clickhouse/client';\n\nconst client = createClient({ url: process.env.CLICKHOUSE_URL! });\n\ninterface RevenueRow {\n  day: string;\n  region: string;\n  revenue: number; // wrong: ClickHouse returns this as a string\n}\n\nasync function revenueByRegion(tenantId: string) {\n  const result = await client.query({\n    query: `\n      SELECT toStartOfDay(created_at) AS day,\n             region,\n             sum(total) AS revenue\n      FROM orders\n      WHERE tenant_id = {tenantId:String}\n      GROUP BY day, region\n      ORDER BY day ASC\n    `,\n    query_params: { tenantId },\n    format: 'JSONEachRow',\n  });\n  return (await result.json()) as RevenueRow[];\n}\n```\n\nThat interface is a promise nobody enforces. `revenue` is typed as `number`, but `sum()` of a numeric column comes back from ClickHouse as a **string** in JS. Rename `total` and the string keeps compiling. The `as RevenueRow[]` cast papers over all of it.\n\nNow the same query with hypequery:\n\n```ts\nimport { createQueryBuilder } from '@hypequery/clickhouse';\nimport type { IntrospectedSchema } from './generated/schema.js';\n\nconst db = createQueryBuilder<IntrospectedSchema>({\n  url: process.env.CLICKHOUSE_URL!,\n  username: process.env.CLICKHOUSE_USERNAME!,\n  password: process.env.CLICKHOUSE_PASSWORD,\n  database: process.env.CLICKHOUSE_DATABASE!,\n});\n\nasync function revenueByRegion(tenantId: string) {\n  return db\n    .table('orders')\n    .where('tenant_id', 'eq', tenantId)\n    .select(['region'])\n    .groupByTimeInterval('created_at', '1 day')\n    .sum('total', 'revenue')\n    .groupBy('region')\n    .orderBy('created_at', 'ASC')\n    .execute();\n}\n```\n\nNo hand-written interface. The result type is inferred from the generated schema — including that `revenue` is a string, because [that's what ClickHouse actually returns](/type-safe-sql-typescript). `tenant_id` is parameterized by construction; there's no template literal to get wrong. Rename `total` in ClickHouse, re-run `hypequery generate`, and `.sum('total', ...)` stops compiling — in your editor, before the code ships.\n\n## The builder has edges, and it tells you where they are\n\nThis is the part that makes the tradeoff fair: hypequery doesn't pretend to cover every ClickHouse feature. Window functions aren't first-class builder methods. You express them with `selectExpr`, which drops a raw SQL expression into a typed `.select()`:\n\n```ts\nimport { createQueryBuilder, selectExpr } from '@hypequery/clickhouse';\n\nconst ranked = await db\n  .table('orders')\n  .select([\n    'region',\n    selectExpr(\n      'ROW_NUMBER() OVER (PARTITION BY region ORDER BY sum(total) DESC)',\n      'rank',\n    ),\n  ])\n  .groupBy('region')\n  .execute();\n```\n\nAnd `.withCTE()` takes a raw SQL string when a subquery is easier written by hand than built. The point of these escape hatches is that adopting the builder is not an all-or-nothing bet. You get compile-time safety on the 90% of queries the builder covers cleanly, and raw SQL for the 10% where SQL is genuinely the better tool — in the same query, side by side. You never have to abandon the typed layer just because one clause is exotic.\n\n## The honest comparison\n\n| | Raw SQL strings | hypequery |\n|---|---|---|\n| Best for | One-off scripts, migrations, gnarly analytics | Reused, app-embedded queries |\n| Schema drift | Silent until runtime | Fails to compile |\n| Refactoring | String search only | Find-references, rename-symbol |\n| Result types | Hand-written, unenforced | Generated from live schema |\n| Runtime type mapping | You remember it (UInt64 → string) | Encoded in generated types |\n| Injection | Safe only if you never interpolate | Parameterized by construction |\n| Reuse | Copy-paste | Composable query definitions |\n| Exotic SQL | Native | `selectExpr` / `rawAs` / `withCTE` |\n\n## Getting started\n\nIf your queries are one-offs, keep writing SQL — that's the right tool, and hypequery would just be in the way. If they live in your app and keep changing, the [quick start](/docs/quick-start) takes you from schema introspection to a typed query and a served endpoint in a few minutes. From there, the [ClickHouse query builder](/clickhouse-query-builder) guide covers filters, joins, and aggregations against generated types, and [migrating raw SQL to hypequery](/blog/migrating-raw-sql-to-hypequery) walks through converting an existing string-based codebase one query at a time — keeping the raw-SQL escape hatch wherever it still makes sense."
  }
};
