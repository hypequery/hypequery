---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3 not-prose my-6 ${className}`}
>
  <slot />
</div>

